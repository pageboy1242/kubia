apiVersion: v1
kind: Pod
metadata:
  name: kubia-liveness
spec:
  containers:
  - image: pageboy/kubia
    name: kubia
    livenessProbe:
      httpGet:
        path: /
        port: 8010
      initialDelaySeconds: 15